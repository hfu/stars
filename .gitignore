# Local data files
data/

# Local config overrides
config/martin.yaml

# Tunnel credentials and secrets (never commit)
secrets/
config/cloudflared/config.yaml

# Personal Claude Code permission overrides
.claude/settings.local.json
